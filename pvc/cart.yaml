---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: cart-pvc
  namespace: default
spec:
  volumeName: cart-pv
  storageClassName: standard
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
